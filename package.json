{
  "name": "bind-std-to-console",
  "version": "1.0.2",
  "description": "Run node with --inspect and see what is in your stdout and stderr. Simple like first import in your app boot.",
  "main": "bind-std-to-console.js",
  "scripts": {
    "start": "nodemon --inspect --expose-gc dev.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "stdout",
    "stderr",
    "bind",
    "console",
    "log"
  ],
  "author": "Gabriel Reitz Giannattasio <g@gartz.me>",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.14.12"
  }
}
